{
    "scripts": {
        "dev": "npm install && npm run dev",
        "build_command": "npm install && npm run build",
        "start_command": "npm install && npm run start"
    },
    "engines": {
        "node": "20.18.3"
    }
}